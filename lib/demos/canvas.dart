import 'package:flutter/material.dart';

class DemosCanvas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text('Canvas Demo')
      ),
      body: Center(child: Text('Canvas Page'),)
    );
  }
}